import React from 'react';

// Note: This is a duplicate file based on the instructions.
// It's generally better to place reusable components in a single location like 'commons'.
const Button: React.FC = () => {
  return (
    <button>Layout Button</button>
  );
};

export default Button;